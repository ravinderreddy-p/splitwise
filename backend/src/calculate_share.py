def calculate_share_per_receiver(expense_amount, list_of_receivers):
    number_of_receivers = len(list_of_receivers)
    share_per_receiver = float(expense_amount) / number_of_receivers
    return share_per_receiver


class CalculateShare:
    pass
